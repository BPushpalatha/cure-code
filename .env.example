# Environment configuration for local development
# Copy this file to `.env` and fill in real values.

# Required: Postgres connection string used by Prisma and API routes
# Example format (adjust host, db, user, password):
# postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<database>?sslmode=require

# Optional: Public URL for the external signaling server used by Emergency page
# If not set, client may fallback to http://localhost:4000 in development
NEXT_PUBLIC_SOCKET_URL=https://<your-signaling-server-domain>

# Optional: Example placeholder used during Vercel project creation
EXAMPLE_NAME=I9JU23NF394R6HH
# Local development defaults
DATABASE_PROVIDER=sqlite
DATABASE_URL="file:./prisma/prisma/dev.db"

# Client-side URL for emergency signaling server
# For production, set this to your external signaling server (e.g., Railway/Render)
NEXT_PUBLIC_SOCKET_URL="http://localhost:4000"
